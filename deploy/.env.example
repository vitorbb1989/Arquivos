# Exemplo de variáveis de ambiente para deploy
# Copie este arquivo para .env e ajuste os valores

# Domain para Traefik
DOMAIN=api.comandos.seudominio.com

# Senha do PostgreSQL
DB_PASS=TroquePorUmaSenhaForte123!

# Chave mestra do Meilisearch
MEILI_MASTER_KEY=TroquePorUmaChaveForte456!

# Log level (debug, info, warning, error)
LOG_LEVEL=info

# Opcional: configurações de backup
BACKUP_DIR=/var/backups/cmdgen
RETENTION_DAYS=7
